import styled from 'styled-components';

const Title = styled.div`
  display:flex;
  flex-direction: column;
  max-width: 400px;
  margin-right: auto;

  & > p {
    margin-top: 3em;
  }
`;

export default Title;
